import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "SocialFlow AI — AI-Powered Social Media Scheduling",
    template: "%s | SocialFlow AI",
  },
  description: "Generate viral AI content and schedule to all your social platforms. Save 10+ hours per week.",
  keywords: ["social media scheduling", "AI content generator", "Instagram scheduler", "social media tool"],
  openGraph: {
    title: "SocialFlow AI",
    description: "Generate viral AI content and schedule to all your social platforms.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
      suppressHydrationWarning
    >
      <body className="min-h-full">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
